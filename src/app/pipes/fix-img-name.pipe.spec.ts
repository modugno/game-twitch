import { FixImgNamePipe } from './fix-img-name.pipe';

describe('FixImgNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FixImgNamePipe();
    expect(pipe).toBeTruthy();
  });
});
