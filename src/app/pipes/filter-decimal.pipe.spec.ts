import { FilterDecimalPipe } from './filter-decimal.pipe';

describe('FilterDecimalPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDecimalPipe();
    expect(pipe).toBeTruthy();
  });
});
